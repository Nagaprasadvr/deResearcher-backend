To install dependencies:

```sh
bun install
```

To run:

```sh
bun run dev
```

DB

Migrate:

```sh
bun run db:generate
```

Push:

```sh
bun run db:push
```

Deploy:

```sh
bun run db:migrate
```

Studio:

```sh
bun run db:studio
```

open http://localhost:5000
